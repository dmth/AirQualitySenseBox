#ifdef sos

byte host[] = {10,64,1,10}; //could also be char* host = "mySOS.url";
char* sospath = "/52nSOSv3.5.0/sos";

/*
  Read the template file, and build one observation from it. append this observation to a tmp file.
   returns 0 if succesful
 */
int buildObservation(char* hash, void* value, char* uom, struct message smsg){
  
  
  
  return 0;
}

#endif
